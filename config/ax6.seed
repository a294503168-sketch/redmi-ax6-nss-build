# Redmi AX6 .config 种子
#
# 这不是完整的 .config，只是差异项。scripts/build.sh 会把它拷成 .config
# 再跑 make defconfig 展开成完整配置。
# 基线参考上游的 nss-setup/config-nss.seed，删掉了里面一堆用不上的插件。

# ============================================================
# 目标平台与设备
# ============================================================
CONFIG_TARGET_qualcommax=y
CONFIG_TARGET_qualcommax_ipq807x=y
CONFIG_TARGET_qualcommax_ipq807x_DEVICE_redmi_ax6=y

# ============================================================
# 构建选项（沿用上游 NSS seed 的关键项）
# ============================================================
CONFIG_CCACHE=y
CONFIG_DEVEL=y
CONFIG_EXPERIMENTAL=y
CONFIG_TOOLCHAINOPTS=y
CONFIG_TARGET_OPTIONS=y
CONFIG_TARGET_OPTIMIZATION="-O2 -pipe -mcpu=cortex-a53+crc+crypto"
CONFIG_USE_GC_SECTIONS=y
CONFIG_ZLIB_OPTIMIZE_SPEED=y
CONFIG_ZSTD_OPTIMIZE_O3=y

# NSS 要求内核不抢占，否则 NPU 数据面会抖
CONFIG_KERNEL_PREEMPT_NONE=y
CONFIG_KERNEL_PREEMPT_NONE_BUILD=y

# 上游 seed 默认打开的内核调试项，我们不调试内核，关掉省时间
# CONFIG_COLLECT_KERNEL_DEBUG is not set
# CONFIG_KERNEL_DYNAMIC_DEBUG is not set

# ============================================================
# NSS 硬件卸载
# ============================================================
# 核心驱动与连接管理器（target.mk 里已是默认包，这里显式声明便于审查）
CONFIG_PACKAGE_kmod-qca-nss-drv=y
CONFIG_PACKAGE_kmod-qca-nss-ecm=y
CONFIG_PACKAGE_kmod-qca-nss-dp=y

# PPPoE 卸载 —— 千兆 PPPoE 拨号跑满线速靠这个
CONFIG_PACKAGE_kmod-qca-nss-drv-pppoe=y

# 桥接与 VLAN 卸载
CONFIG_PACKAGE_kmod-qca-nss-drv-bridge-mgr=y
CONFIG_PACKAGE_kmod-qca-nss-drv-vlan-mgr=y

# ============================================================
# 剔除全部无线内容（本设备不使用 WiFi）
# scripts/strip-wifi.sh 会同时把它们从 target 的 DEFAULT_PACKAGES /
# DEVICE_PACKAGES 里摘掉，双保险。
# ============================================================
# CONFIG_PACKAGE_kmod-ath11k is not set
# CONFIG_PACKAGE_kmod-ath11k-ahb is not set
# CONFIG_PACKAGE_kmod-ath11k-pci is not set
# CONFIG_PACKAGE_ath11k-firmware-ipq8074 is not set
# CONFIG_PACKAGE_ipq-wifi-redmi_ax6 is not set
# CONFIG_PACKAGE_kmod-cfg80211 is not set
# CONFIG_PACKAGE_kmod-mac80211 is not set
# CONFIG_PACKAGE_wpad-basic-mbedtls is not set
# CONFIG_PACKAGE_wpad-mesh-openssl is not set
# CONFIG_PACKAGE_kmod-qca-mcs is not set
# CONFIG_PACKAGE_kmod-qca-nss-drv-wifi-meshmgr is not set
# CONFIG_ATH11K_NSS_MESH_SUPPORT is not set

# ============================================================
# Web 界面（与官方默认固件一致：luci 元包）
# ============================================================
CONFIG_PACKAGE_luci=y

# ============================================================
# 中文化
#
# 不要写 CONFIG_PACKAGE_luci-i18n-xxx-zh-cn=y —— luci.mk 里这些包是
#   HIDDEN:=1
#   DEFAULT:=LUCI_LANG_$(lang)||(ALL&&m)
# HIDDEN 的符号不能在 .config 里直接选，make defconfig 会静默丢弃。
# 正确开关是语言总开关，它会让所有已选 luci 包自动带上对应语言包。
#
# 语言名取自 po 目录名 zh_Hans（不是 zh-cn，后者只是 LUCI_LC_ALIAS 出来的包名后缀）。
# ============================================================
CONFIG_LUCI_LANG_zh_Hans=y

# ============================================================
# 额外软件包
# ============================================================
CONFIG_PACKAGE_btop=y
CONFIG_PACKAGE_htop=y
CONFIG_PACKAGE_iperf3=y
CONFIG_PACKAGE_ttyd=y
CONFIG_PACKAGE_zsh=y
CONFIG_PACKAGE_luci-app-ttyd=y
